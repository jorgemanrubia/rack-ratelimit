source 'https://rubygems.org'

gemspec

# For tests only.
gem 'rake'
gem 'minitest'
gem 'sinatra'
gem 'rack-test'
gem 'benchmark-ips'

# Optional dependencies. Use Memcached or Redis.
gem 'dalli'
gem 'redis'
